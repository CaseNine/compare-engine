define(function () {

    /**
     * @deprecated Because now we use
     *  instanceof functionality.
     * @enum {number}
     */
    var TokenKind = {
        ALPHA_NUMERIC: 0,
        NON_ALPHA_NUMERIC: 1
    };

    return TokenKind;

});
